apiVersion: v1
kind: Service
metadata:
  name: {{ include "digits-inference.fullname" . }}
  labels:
    {{- include "digits-inference.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "digits-inference.selectorLabels" . | nindent 4 }}